/**
 * Props for the custom skeleton loader component.
 */
export interface CustomSkeletonType {
    /**
     * Number of skeleton rows to display.
     */
    rowCount?: number

    /**
     * Flag to stop horizontal scrolling
     */
    stopHorizontalScrolling?: boolean

    /**
     * Flag to stop vertical height adjustment.
     */
    stopVh?: boolean
}
